# 数学建模项目全局要求配置

## 一、数据规范

### 1.1 文件命名
- 原始数据: `数据集一/` 目录
- 清洗代码: `清洗代码一/` 目录 (中文命名)
- 清洗数据: `清洗数据一/` 目录 (中文表头CSV)
- 输出结果: `输出结果/分配结果CSV/` 目录
- 说明文档: 每个文件夹内放简洁的md说明

### 1.2 输出格式
- 优先CSV (utf-8-sig编码), 非必要不输出Excel
- 表头使用中文
- 每张表独立CSV文件

### 1.3 可视化规则
- 使用全量数据绘图 (禁止采样导致空隙)
- 每张图下方加文字说明解释图含义
- 配色方案: E1=#2196F3蓝, E3=#FF9800橙, E4=#4CAF50绿
- 图片dpi≥150, 保存为PNG

## 二、仓库物理参数

### 2.1 货架结构
```
总货架数: 14个 (7巷道×2面)
每货架: 50层 × 68列 = 3400货位
总货位: 14×50×68×2(深/浅) = 95,200 箱位
双深位: 每个(x,z)有深位+浅位两个存储位置
```

### 2.2 坐标定义
```
x: 列深度, 范围[1, 68], 68列
y: 货架序号, 范围[1, 14], 14个货架
z: 层高度, 范围[1, 50], 50层
原点(0,0): 货架左下角第1列第1层巷道口
距离度量: x+z (α=β, 横向纵向权重相同)
```

### 2.3 层高与箱型约束
```
第1-8层:   高度200mm, 仅可存放E1箱
第9-42层:  高度400mm, 可存放E1、E3箱
第43-50层: 高度500mm, 可存放E1、E3、E4箱
```

### 2.4 堆垛机参数
```
空载水平速度: 3.0 m/s, 加速度: 0.5 m/s²
空载垂直速度: 0.75 m/s, 加速度: 0.15 m/s²
满载水平速度: 2.3 m/s, 加速度: 0.4 m/s²
满载垂直速度: 0.58 m/s, 加速度: 0.10 m/s²
货叉操作时间: 3.0 s
附加时间(扫码/定位/通信): 3.0 s
每列宽度: 0.6 m
```

## 三、问题一: 库位分配优化约束

### 3.1 填充优先级
```
核心原则: 同一(x,z)先填满全部14个货架(y=1~14), 再移动到下一个(x,z)
排序依据: x+z距离从小到大
同距离时: 低层优先, 前列优先
```

### 3.2 消耗占比规则
```
消耗占比越高的材料 → 优先分配最优位置(最小x+z)
即: 第1批放在(x=1,z=1, y=1~14)的材料是消耗占比最高的
```

### 3.3 双深位规则
```
材料库存为偶数: 深浅位同时存放 (同一(x,z,y)的深+浅)
材料库存为奇数: 多出的1箱仅放浅位, 深位空置
验证: 浅位数 - 深位数 = 奇数库存材料种类数
```

### 3.4 校验清单
- [ ] 货物总量=65000箱, 全部分配, 无遗漏
- [ ] 1-8层仅E1, 9-42层无E4, 43-50层全类型
- [ ] x∈[1,68], y∈[1,14], z∈[1,50]
- [ ] 各货架分配数均匀 (差异<5%)
- [ ] 无孤立的深位(每个深位必须有对应浅位)
- [ ] 消耗占比与距离负相关 (高消耗→近距离)
- [ ] 各材料分配数=原始库存数

## 四、出库时间公式

```
线性模型:
  T_get  = max( x·w / v_xe , H(z) / v_ze )
  T_back = max( x·w / v_xl , H(z) / v_zl )
  T = T_get + T_back + t_fork + t_other

加速度模型:
  若 dist ≤ v²/a → 三角形速度剖面: t = 2·√(dist/a)
  若 dist > v²/a → 梯形速度剖面:   t = 2·v/a + (dist - v²/a)/v
```

## 五、开发规范

### 5.1 代码组织
- 清洗代码: 单独脚本, 输出到清洗数据目录
- 建模代码: 每个问题独立脚本
- 可视化代码: 独立脚本, 与建模分离
- 校验代码: 每个问题完成后运行全量校验

### 5.2 通用规则
- 编码: UTF-8
- 依赖: numpy, pandas, scipy, matplotlib, openpyxl
- 进度输出: 大数据量处理时输出进度百分比
- 错误处理: 校验不通过必须修复, 不能忽略
- 临时文件: 用完即删

### 5.3 图片输出标准
- 字体: Microsoft YaHei (中文), DejaVu Sans (英文)
- 关闭Unicode负号bug: axes.unicode_minus=False
- 图中必须有: 标题、轴标签、图例、必要的文字说明
- 避免: 采样失真、颜色过淡、标注缺失
