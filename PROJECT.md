# 库位分配优化问题 - 项目配置说明

## 一、赛题概述

**题目**：A题 - 库位分配优化问题（第26届电子科技大学数学建模竞赛）

**场景**：S工厂自动立体库（AS/RS），7巷道、14货架、7堆垛机，双深位货架（50层×68列），约2000种原材料。

**核心目标**：优化库位分配与堆垛机调度，最小化出入库作业总时间。

## 二、项目结构

```
A题/
├── A题：库位分配优化问题.docx    # 赛题描述文档
├── A题：附件 数据集/              # 原始数据集
│   ├── 表1_原材料库存数据.csv     # 2000种原材料（编号/箱型/消耗占比/库存量）
│   ├── 表2_生产线订料数据.csv     # 2021/6/1-6/7订料记录（3738条）
│   └── 表3_入库材料数据.csv       # 同期入库记录（667条）
├── 数据集一/                      # 数据集副本（处理用）
│   ├── 表1_原材料库存数据.csv
│   ├── 表2_生产线订料数据.csv
│   └── 表3_入库材料数据.csv
├── 清洗代码一/                    # 数据预处理代码
├── 清洗数据一/                    # 清洗后数据输出
├── .gitignore
├── PROJECT.md                     # 本文件
└── requirements.txt               # Python依赖
```

## 三、问题分解

| 问题 | 内容 | 核心方法 |
|------|------|----------|
| 问题一 | 库位初始分配优化 | 多目标优化，考虑消耗频率+箱型约束+双深位约束 |
| 问题二 | 纯出库堆垛机调度 | 调度优化，死锁检测与解决，多策略对比 |
| 问题三 | 出入库协同优化 | 复合作业模式，协同调度 |
| 问题四 | 非线性速度模型 | 运动学建模（加速度约束），对比线性模型 |
| 问题五 | 动态库位重分配 | 自适应策略，重分配触发条件与成本分析 |

## 四、关键参数

### 货架参数
- 货架数：14个（7巷道×2）
- 每货架：50层 × 68列 = 3400货位
- 总货位：14 × 3400 = 47,600（双深位，实际存储约47,600箱）
- 编号格式：`货架编号_层_列_深浅位`（如 `01_54_06_02`）

### 箱子类型与层高约束
| 层范围 | 高度 | 可存箱型 |
|--------|------|----------|
| 1-8层 | 200mm | E1 |
| 9-42层 | 400mm | E1, E3 |
| 43-50层 | 500mm | E1, E3, E4 |

### 堆垛机速度参数
| 状态 | 水平最大速度 | 垂直最大速度 | 水平加速度 | 垂直加速度 |
|------|-------------|-------------|-----------|-----------|
| 空载 | 3.0 m/s | 0.75 m/s | 0.5 m/s² | 0.15 m/s² |
| 满载 | 2.3 m/s | 0.58 m/s | 0.4 m/s² | 0.10 m/s² |

## 五、数据文件字段说明

### 表1 - 原材料库存数据
- `原材料编号`：B6T开头的13位编码
- `箱子类型`：E1 / E3 / E4
- `消耗占比`：浮点数，所有原材料消耗占比之和≈1
- `库存数量/箱`：当前库存箱数

### 表2 - 生产线订料数据
- `订单号`：整数序号
- `箱号`：唯一箱标识
- `箱子类型`：E1 / E3 / E4
- `原材料号`：对应表1的原材料编号
- `单位`：箱
- `时间`：需求时间（2026/6/1 ~ 2026/6/7）

### 表3 - 入库材料数据
- `入库批次`：IN开头批次号
- `原材料编号`：对应表1的原材料编号
- `箱子类型`：E1 / E3 / E4
- `入库数量/箱`：入库箱数
- `入库时间`：入库时间

## 六、环境配置

### Python版本
Python 3.9+

### 核心依赖
```
numpy          # 数值计算
pandas         # 数据处理
scipy          # 优化算法
matplotlib     # 可视化
seaborn        # 统计可视化
ortools        # OR-Tools优化求解器（可选）
pulp           # 线性规划（可选）
gurobipy       # Gurobi求解器（可选，需许可证）
openpyxl       # Excel读写
python-docx    # Word文档读写
```

## 七、开发约定

1. **编码**：UTF-8
2. **数据处理**：先清洗再建模，清洗代码放`清洗代码一/`，清洗结果放`清洗数据一/`
3. **建模顺序**：按问题一→五依次推进，问题一的结果是后续问题的基础
4. **论文**：最终输出PDF，含摘要、问题分析、模型假设、模型建立、求解算法、结果分析、结论
5. **算法要求**：需给出复杂度分析和收敛性说明（如适用）

## 八、出库规则要点

1. 取浅位：直接叉取→运至出口
2. 取深位：叉取深浅两箱→浅箱暂存对面空位（优先放深位）→深箱运至出口（"倒腾"操作）
3. 如果对面货架无空位，深位料箱无法取出（死锁风险）
4. 同原材料深浅位存放时：订2箱一并取出，订1箱优先取浅位
